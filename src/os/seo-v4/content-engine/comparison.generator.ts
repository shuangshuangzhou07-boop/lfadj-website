export function generateComparison(topicA: string, topicB: string): string[] {
  return [
    `${topicA} advantages`,
    `${topicB} advantages`,
    "Runtime and maintenance comparison",
    "Best-fit applications",
    "Buyer decision summary",
  ];
}

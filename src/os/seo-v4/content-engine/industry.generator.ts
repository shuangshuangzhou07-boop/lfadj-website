export function generateIndustryUseCases(industry: string): string[] {
  return [
    `${industry} temporary lighting planning`,
    `${industry} night work safety support`,
    `${industry} mobile light tower deployment`,
  ];
}
